local log = require('utils.log')
local format = require('utils.format')

local midi_patterns = {}

midi_patterns.insertPatternForCurrentBarAndNoteRow = function()

  -- midi editor

  -- make sure we are in a midi item

  -- get current note row

  -- create array of sixteen

  -- randombly set indices true/false

  -- insert sixteenth notes in bar at note row
end

return midi_patterns
